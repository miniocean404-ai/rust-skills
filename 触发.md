# 触发机制详解

本文档详细说明 rust-skills 项目如何自动触发元认知和 Hook 路由。

## 🎯 触发机制概览

### 1️⃣ **Hook 触发层** - 自动拦截用户输入

#### 工作流程:

```
用户输入问题
    ↓
hooks/hooks.json 监听 UserPromptSubmit 事件
    ↓
正则表达式匹配 (400+ 关键词)
    ↓
匹配成功 → 执行 Hook 脚本
```

#### 关键文件: `hooks/hooks.json`

```json
{
  "hooks": {
    "UserPromptSubmit": [  // ← 在用户提交问题时触发
      {
        "matcher": "(?i)(rust|cargo|E0\\d{3,4}|ownership|async|...)",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/.claude/hooks/rust-skill-eval-hook.ps1",
            "platforms": ["win32"]  // Windows 用 .ps1
          },
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/.claude/hooks/rust-skill-eval-hook.sh",
            "platforms": ["darwin", "linux"]  // Linux/macOS 用 .sh
          }
        ]
      }
    ]
  }
}
```

**关键点:**
- `UserPromptSubmit`: Claude Code 的 Hook 时机,在用户提交问题时触发
- `matcher`: 正则表达式,匹配 400+ 个关键词(中英文、错误码、概念词)
- `platforms`: 根据操作系统自动选择脚本

#### 匹配的关键词类型:

| 类型 | 示例 |
|------|------|
| 语言关键词 | rust, cargo, crate |
| 错误码 | E0382, E0597, E0277 |
| 概念词 | ownership, borrow, lifetime |
| 并发词 | async, await, Send, Sync |
| 智能指针 | Arc, Rc, Box, Mutex |
| 领域词 | axum, tokio, serde |
| 中文词 | 所有权, 借用, 异步, 并发 |
| 问题词 | 怎么, 如何, 为什么, how, why |

---

### 2️⃣ **元认知强制层** - 注入指令

#### Hook 脚本: `.claude/hooks/rust-skill-eval-hook.sh` (或 .ps1)

这个脚本会在 Claude 收到用户问题**之前**,向 Claude 注入一段强制指令:

```bash
#!/bin/bash
cat << 'EOF'

=== MANDATORY: META-COGNITION ROUTING ===

CRITICAL: You MUST follow the COMPLETE meta-cognition framework.

## STEP 1: IDENTIFY ENTRY LAYER + DOMAIN
- 识别错误码 → Layer 1 技能
- 识别领域关键词 → Layer 3 技能
- 必须同时加载两个技能

## STEP 2: EXECUTE TRACING (MANDATORY)
L1 Error → TRACE UP → L3 Domain → TRACE DOWN → Solution

## STEP 3: MANDATORY OUTPUT FORMAT
必须输出:
- Reasoning Chain (推理链)
- Domain Constraints Analysis (领域约束分析)
- Recommended Solution (推荐方案)

EOF
```

**效果:**
- Claude 收到的不是单纯的用户问题
- 而是: **用户问题 + Hook 注入的元认知指令**
- 强制 Claude 按照指定流程思考和输出

---

### 3️⃣ **路由层** - rust-router 技能

当 Hook 触发后,Claude 会首先调用 `rust-router` 技能进行路由:

#### rust-router 的职责:

```
1. 分析用户问题
   ↓
2. 识别入口层级 (L1/L2/L3)
   - L1: 语言机制错误 (E0382, Send/Sync)
   - L2: 设计选择 (性能优化, 架构)
   - L3: 领域问题 (Web API, CLI, 嵌入式)
   ↓
3. 检测领域关键词
   - Web API → domain-web
   - payment → domain-fintech
   - CLI → domain-cli
   ↓
4. 决策加载哪些技能
   - 单技能: 只有 L1 或 L2
   - 双技能: L1 + L3 (有领域上下文)
```

#### 路由表示例:

| 用户问题 | 识别结果 | 加载技能 |
|---------|---------|---------|
| "E0382 错误怎么解决" | L1: ownership | m01-ownership |
| "Web API 报错 Rc cannot be sent" | L1: concurrency + L3: web | m07-concurrency + domain-web |
| "如何优化 Rust 性能" | L2: performance | m10-performance |

---

### 4️⃣ **完整触发流程示例**

#### 场景: 用户问 "Web API 报错 Rc cannot be sent"

```
┌─────────────────────────────────────────┐
│ 用户输入: "Web API 报错 Rc cannot be sent" │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│ hooks/hooks.json                         │
│ - 匹配 "Web API" ✓                       │
│ - 匹配 "Rc" ✓                            │
│ - 匹配 "Send" ✓                          │
│ → 触发 Hook!                             │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│ rust-skill-eval-hook.sh 执行             │
│ 输出元认知指令:                           │
│ - 强制识别层级和领域                      │
│ - 强制加载 Skills                        │
│ - 强制输出格式                           │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│ Claude 收到:                             │
│ 1. 用户问题                              │
│ 2. Hook 注入的元认知指令                 │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│ Claude 执行 Skill(rust-router)           │
│ 路由分析:                                │
│ - "Rc", "Send" → L1: m07-concurrency    │
│ - "Web API" → L3: domain-web            │
│ → 决策: 双技能加载                       │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│ Claude 加载技能:                         │
│ 1. Skill(m07-concurrency)               │
│ 2. Skill(domain-web)                    │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│ Claude 执行追溯:                         │
│ L1: Rc 不是 Send                         │
│   ↑ TRACE UP                            │
│ L3: Web handlers 必须 Send + Sync       │
│   ↓ TRACE DOWN                          │
│ L2: 使用 Arc<T> + State extractor       │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│ Claude 输出 (按强制格式):                │
│ ### Reasoning Chain                     │
│ +-- Layer 1: Send/Sync Error            │
│ |       ^                               │
│ +-- Layer 3: Web Domain                 │
│ |       v                               │
│ +-- Layer 2: Use Arc<T>                 │
│                                          │
│ ### Domain Constraints Analysis         │
│ ### Recommended Solution                │
└─────────────────────────────────────────┘
```

---

## 🔑 关键设计点

### 1. **为什么能自动触发?**

✅ **UserPromptSubmit Hook**
- Claude Code 的内置机制
- 在用户提交问题时自动执行
- 无需用户手动调用

✅ **400+ 关键词覆盖**
- 几乎所有 Rust 相关问题都会被匹配
- 中英文双语支持
- 错误码正则匹配 (E0\d{3,4})

### 2. **为什么能强制元认知?**

✅ **Hook 脚本注入指令**
- 在 Claude 思考前注入
- 使用 "MANDATORY", "CRITICAL" 等强制词
- 明确要求输出格式

✅ **双技能加载策略**
- 不只是修复编译错误
- 同时考虑领域最佳实践

### 3. **跨平台支持**

✅ **platforms 字段**
- Windows: 自动使用 .ps1
- Linux/macOS: 自动使用 .sh
- 无需用户配置

---

## 📊 触发效果对比

### ❌ 没有 Hook 的情况:

```
用户: "Web API 报错 Rc cannot be sent"
Claude: "Rc 不是 Send,改用 Arc"
```

### ✅ 有 Hook 的情况:

```
用户: "Web API 报错 Rc cannot be sent"

Claude:
### Reasoning Chain
+-- Layer 1: Send/Sync Error
|   Problem: Rc<T> 不能跨线程传递
|       ^
+-- Layer 3: Web Domain (domain-web)
|   Constraint: Web handlers 在任意线程运行
|   Rule: 共享状态必须是 Send + Sync
|       v
+-- Layer 2: Design Choice
    Decision: 使用 Arc<T> + axum State extractor

### Domain Constraints Analysis
从 domain-web 技能:
- "Rc in state" 是常见错误
- Web handlers 要求 Send + Sync
- 推荐: axum State<Arc<T>> 模式

### Recommended Solution
[符合 Web 最佳实践的完整代码]
```

---

## 🎯 总结

这个项目通过 **三层机制** 实现自动触发:

1. **hooks/hooks.json** - 关键词匹配,自动拦截
2. **rust-skill-eval-hook.sh/ps1** - 注入元认知指令
3. **rust-router** - 智能路由,双技能加载

整个过程对用户**完全透明**,只需正常提问,系统会自动:
- ✅ 触发 Hook
- ✅ 加载正确的技能
- ✅ 执行元认知追溯
- ✅ 输出结构化答案

---

## 📁 相关文件

### 核心配置文件
- `hooks/hooks.json` - Hook 触发配置
- `.claude/hooks/rust-skill-eval-hook.sh` - Linux/macOS Hook 脚本
- `.claude/hooks/rust-skill-eval-hook.ps1` - Windows Hook 脚本

### 技能文件
- `skills/rust-router/SKILL.md` - 路由技能
- `skills/m01-m07/` - Layer 1 语言机制技能
- `skills/m09-m15/` - Layer 2 设计选择技能
- `skills/domain-*/` - Layer 3 领域技能

### 文档
- `docs/hook-mechanism-zh.md` - Hook 机制详细文档
- `docs/architecture-zh.md` - 架构设计文档
- `docs/forced-eval-hook.md` - 强制评估 Hook 说明

---

## 🧪 测试触发机制

使用测试脚本验证 Hook 是否正常工作:

**Linux/macOS:**
```bash
./test-triggers.sh
```

**Windows:**
```powershell
.\test-triggers.ps1
```

测试会验证:
- Hook 是否被触发
- 技能是否被正确加载
- 输出格式是否符合要求
